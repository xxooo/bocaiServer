import Vue from 'vue';
import request from './../../../common/request.js';
Vue.use(request);

const state = {

};

const getters = {

  
};

const actions = {


};

// mutations负责修改state
const mutations = {
};

export default {
  state,
  getters,
  actions,
  mutations,
};
